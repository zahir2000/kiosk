import 'package:flutter/material.dart';
import 'package:sizer/sizer.dart';

import '../../utils/index.dart';

Widget oneHandWidget({double height = 8, double width = 8}) => SizedBox(
      height: height.h,
      width: width.w,
      child: Image.asset(oneHandPic),
    );
Widget twoHandWidget({double height = 8, double width = 8}) => SizedBox(
      height: height.h,
      width: width.w,
      child: Image.asset(twoHandPic),
    );
Widget threeHandWidget({double height = 8, double width = 8}) => SizedBox(
      height: height.h,
      width: width.w,
      child: Image.asset(threeHandPic),
    );
Widget fourHandWidget({double height = 8, double width = 8}) => SizedBox(
      height: height.h,
      width: width.w,
      child: Image.asset(fourHandPic),
    );
Widget fiveHandWidget({double height = 8, double width = 8}) => SizedBox(
      height: height.h,
      width: width.w,
      child: Image.asset(fiveHandPic),
    );
Widget okHandWidget({double height = 8, double width = 8}) => SizedBox(
      height: height.h,
      width: width.w,
      child: Image.asset(okHandPic),
    );
Widget okCartHandWidget({double height = 8, double width = 8}) => SizedBox(
      height: height.h,
      width: width.w,
      child: Image.asset(okCartHandPic),
    );
